---
description: Leave the kit path for this chat only. New chats start on-path.
---

# /off-path — this session only

This Cursor chat is **off the kit path** until it ends.

## Rules

- **Do not persist.** Do not write `work/*/kit-path`, do not flip intent to “off-path,” do not add a repo flag. The next chat starts **on-path** again.
- **Warn once** (what artifacts will not be prepared, risk, how to rejoin: new chat or keep talking without this command). Then follow the human. Do not nag.
- Safety stays hard: dirty tree, silent stash/reset, fake verify/accept, silent What/Why change (`_ask/policies/workflow.md`).

## On-path (not this session)

Default for every new session: prepare each stage artifact; one defaults-OK + Accept. `/off-path` does not change that default for other chats.
