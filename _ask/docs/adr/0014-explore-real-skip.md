# 00 Explore may be a real skip

When the destination is already sharp enough for Intent, omit kit `00`. Do not write `explore-map.md`. Record `Explore skipped: destination already clear.` in `intent.md`. `/00-explore` always runs 00. Free on-path chat: announce 00 vs 01 and wait for confirm. `/01-grill` does not override fog. `start-work` does not seed the explore-map. `01`–`10` still require artifacts; skip there means extra approvals only.

Policy: `_ask/policies/workflow.md`. Spec: `specs/current/skip-explore.md`.
