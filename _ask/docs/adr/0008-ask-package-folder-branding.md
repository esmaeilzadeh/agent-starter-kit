# Kit root folder is _ask/

The portable kit protocol tree is branded `_ask/` (not `engineering/`). All Guide modules, Build Spec modules, agent contracts, policies, templates, and skills manifest live under that root so the kit is recognizable in consuming repos.
