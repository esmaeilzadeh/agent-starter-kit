# Review

## Scope

`specs/current/promote-kit-specs.md` vs `_ask/spec/` inserts and product-spec pointers.

## Findings

None. Contracts landed in existing modules only (no new `_ask/spec/` file). Product specs are pointers; `demo-nn-train` still owns the trainer/viewer.

## Suggested fixes

None.

## Residual risks

Guide prose in `_ask/guide/` was not rewritten; mapping tables now point at the promoted sections.

## Review verdict

PASS
