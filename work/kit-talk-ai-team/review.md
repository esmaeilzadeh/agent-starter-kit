# Review

## Scope

`_ask/docs/talks/method-and-tooling.html`, `_ask/docs/talks/README.md`, `_ask/docs/README.md` against `specs/current/kit-talk-ai-team.md`. Compared to `8b7238d`.

## Findings

1. Spec behavior is present: keyboard next/prev/home/end, counter, notes on `n`, no CDN, method then tooling, one Demo kicker, three-verb table with distinct objects, `sync` named as a fourth verb, demo points at `_ask/docs/demo/end-to-end-plan.md`.
2. `setup` is human-only / TTY; `install` is another repo; `prepare` is the manifest. Matches `./ask` help.
3. Density: 24 slides for 90 minutes leaves little slack if the room debates ownership. Notes already front-load the three-verb slide (10 minutes). Residual, not a spec miss.

## Suggested fixes

None required for Accept. If a rehearsal overruns, drop the "this week" slide into discussion rather than cutting the three-verb slide.

## Residual risks

A presenter who has not read the Guide will run out of answers on Lean/Prolog/acceptance-debt follow-ups. Those topics are out of the talk on purpose.

## Review verdict

PASS
