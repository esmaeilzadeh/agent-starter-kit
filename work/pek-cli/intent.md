# Intent: pek root dispatcher

## What

Single root command `pek` that execs kit scripts under `_ask/`.

## Why

Avoid a generic `scripts/` name while keeping one easy entry for humans and agents.

## Non-goals

Reintroducing root `scripts/`. Wrapping product apps.

## Human decisions

Name: `pek` (Part Engineering Kit).
